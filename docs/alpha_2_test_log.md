# Alpha 2 Test Log

Record each build, runtime proof, regression, and accepted deferral here. Evidence must identify an output file, console capture, deterministic fixture, or commit; vague observations are not sufficient release evidence.

| Date | Profile / seed | Map | Build | Reproduction steps | Expected result | Actual result | Evidence | Severity | Resolution |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-10 | preview / 1337 | `zn_preview` | Walker Phase 2 | Build `mingw-debug`; run CTest; load preview and submit one noise command. | Native worker runs at 4 Hz and accepts the command. | Passed: 1/1 CTest; worker accepted noise without API errors. | `ctest --preset test-mingw-debug`; `DATA/zombiesim/walker_status.json` | none | Baseline accepted before Phase B. |
| 2026-09-10 | preview / 1337 | `zn_ra_rs-h-d1_n-m22-c2a-p3-0-q0-cp` | Plan 0 | Run `nav_generate` on the staged recipe map. | At least one nav area is saved and reloads. | Failed: generation completed with zero areas and wrote an invalid 28-byte `.nav` file. | Server console; `maps/preview/zn_ra_rs-h-d1_n-m22-c2a-p3-0-q0-cp.nav` | major | Deferred: normalize authored player-start placement before broad navmesh coverage. This does not block Phase A or Phase B. |
| 2026-09-10 | preview / 1337 | portable core fixture | Phase B | Clean-build the core and Win64 module; run CTest. | API v2 ticket adapter compiles; request replay, duplicate terminal commands, capacity bounds, and terminal retention are covered. | Passed: core CTest 1/1 and `gmsv_zombiesimwalker_win64.dll` linked. | `ctest --preset test-mingw-debug --output-on-failure`; `dist/gmsv_zombiesimwalker_win64.dll` | none | Native contract accepted; live server ticket lifecycle test remains pending. |
| 2026-09-10 | preview / 1337 | `zn_safe-zone_rt-mw-d1_n` | Phase B | Run the ticket request probe from a safe-zone cell. | The server rejects a safe-zone request before it reaches the worker. | Initial probe was queued, then yielded no ticket because cell 288 is a safe zone. | Server console: active cell 288; runtime cell `safeZone: 9`. | minor | Resolved: the server wrapper now rejects safe-zone ticket requests synchronously. |
| 2026-09-10 | preview / 1337 | `zn_st_rx-a-d2_n-v2` | Phase B | Run `zombiesim_walker_ticket_probe` on ordinary cell 313. | One exact ticket moves from reserved to materialized to despawned; population is unchanged. | Passed: ticket `1:0` reserved at tick 313, materialized at 317, despawned at 321; population remained 81,000. | `DATA/zombiesim/walker_ticket_probe.json` | none | Phase B live despawn path accepted. Killed-path proof remains Phase C work because it requires an entity death. |
| 2026-09-10 | preview / 1337 | `zn_dx_rt-me-d1_ep-m33-e1a-p1-1-q0` | Phase C | Run `zombiesim_walker_zombie_spawn_test` after the navmesh safety search was widened to 128 bounded candidates. | One ticket-backed zombie spawns on a valid nav area without relaxing clearance, player-distance, or occlusion checks. | Passed: ticket `1:0` acknowledged as entity 18 at `(-1537.5, 937.5, 32.76)` in cell 350. Pursuit did not reach the player in the observed run. | `DATA/zombiesim/walker_zombie_spawn.json`; `DATA/zombiesim/walker_zombie_lifecycle.json` | minor | Manual materialization accepted. AI diagnosis deferred to Phase E by operator request; killed and despawned evidence remain pending. |
| 2026-09-10 | preview / 1337 | `zn_dx_rt-me-d1_ep-m33-e1a-p1-1-q0` | Phase D | Let the automatic controller materialize multiple tickets. | Each live zombie receives a distinct, non-overlapping spawn position. | Initial run failed: two zombies shared one nav-area center. Retest passed: controller and native snapshot agreed on 6/6 materialized, with no pending request or controller errors. | In-game observation; `DATA/zombiesim/walker_materializer_status.json`; `DATA/zombiesim/walker_tickets.json`; `DATA/zombiesim/walker_status.json` | none | Resolved: placement rejects positions within 96 units of an existing `zn_walker_zombie`; live controller validation accepted. |
| 2026-09-10 | preview / 1337 | `zn_ra_rx-a-d1_n-m22-c2aa-p0-0-q1-v2` | Phase E | Approach the six materialized zombies, allow pursuit/melee, then run `zombiesim_walker_zombie_status`, materializer status, ticket, and worker diagnostics. | Zombies acquire eligible nearby players, reuse bounded paths, attack only in range with sight, recover from being stuck, and preserve worker cadence at the active cap. | Passed: six active walkers completed 371 staggered target scans, 174 bounded path computes, 92 attacks, and 3 stuck recoveries with no path failures. Controller reported 6/6 active and zero errors; worker had zero dropped ticks/API errors at tick 300. | `DATA/zombiesim/walker_zombie_status.json`; `DATA/zombiesim/walker_materializer_status.json`; `DATA/zombiesim/walker_tickets.json`; `DATA/zombiesim/walker_status.json` | none | Phase E live acceptance complete. Explicit obstructed-path failure evidence remains a non-blocking coverage gap. |
| 2026-09-10 | preview / 1337 | portable core fixture | Phase G | Build the core and API v3 module; run CTest after worker-checkpoint and ticket-allocation changes. | Killed tickets remain terminal across core checkpoint round-trip and native changes preserve deterministic tests. | Passed: `ctest --preset test-mingw-debug --output-on-failure` reported 1/1; both core and Win64 module builds completed. | CTest preset; `dist/gmsv_zombiesimwalker_win64.dll` | none | Accepted. Periodic exports were changed from synchronous game-thread waits to worker-thread request/poll after the initial stutter finding. |
| 2026-09-10 | preview / 1337 | cell 268 then cell 454 | Phase G | Kill ticket `0:53`, save a checkpoint, change level, then restart the local preview server and collect worker/checkpoint/materializer diagnostics. | Population reduces once for death, survives both restore paths, imported materialized tickets reconcile, and an ordinary populated cell resumes ticket-backed spawning. | Passed: population changed `81,000` to `80,999`; checkpoint state hash was `4998178043665575794`; after `changelevel`, cell 454 restored `80,999` and reconciliation completed. A legacy request-id collision initially starved cell 454; persisting/migrating the request epoch resolved it and the cell reached `64/64` active. Full restart again restored `80,999`, with zero API errors/dropped ticks and materializer `45/41`. | `DATA/zombiesim/walker_status.json`; `DATA/zombiesim/walker_checkpoint_status.json`; `DATA/zombiesim/walker_materializer_status.json`; `DATA/zombiesim/walker_tickets.json` | none | Alpha 2 persistence and active-cell spawn acceptance complete. Checkpoints use the profile-scoped server SQLite `walker_checkpoints` table because large `DATA` file writes were not readable in this environment. |
| 2026-09-10 | preview / 1337 | active materialization maps | Alpha 3 deferral | Review zombie behavior after live Alpha 2 validation. | Alpha 2 zombies remain bounded, visible-range, slow walkers; sophisticated sensory, animation, and combat behavior moves to the next milestone. | Accepted by operator. | `entities/entities/zn_walker_zombie/*` | none | Deferred to Alpha 3. |